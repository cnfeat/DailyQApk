# 日课一问 — UI/UX 评估与美化方案 v2

> 评估日期：2026-05-27 | 版本：v2（根据反馈修订）

---

## 一、现状评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 视觉风格 | ★★★★☆ | 森林绿主调，暖金点缀，品位在线 |
| 主题适配 | ★★★★★ | 完整的 Light/Dark 双色方案 |
| 信息层级 | ★★★☆☆ | 主问题和拓展文字一起显示，但视觉区分不够 |
| 交互动效 | ★★☆☆☆ | 基本无动画，切换生硬 |
| 手势操作 | ★☆☆☆☆ | 纯按钮点击，无滑动手势 |
| 触觉反馈 | ★★★☆☆ | 有振动反馈，但只有一种模式 |
| 桌面 Widget | ★★★☆☆ | 显示良好，但不可交互换题 |
| 可分享性 | ★☆☆☆☆ | 无法保存/分享问题卡片 |
| 新用户引导 | ★☆☆☆☆ | 无 onboarding，打开即用 |

---

## 二、UI/UX 改进方案

### 🔥 P0 — 高优先级

#### 1. 卡片切换动画 ✅

```kotlin
AnimatedContent(
    targetState = currentQuestion.question,
    transitionSpec = {
        slideInHorizontally { width -> width } + fadeIn() togetherWith
        slideOutHorizontally { width -> -width } + fadeOut()
    },
    label = "question_transition"
) { questionText ->
    // 渲染问题 + 拓展文字
}
```

**效果**：每次换题，旧问题向左滑出淡出，新问题从右滑入淡入，强化「翻卡片」的心理信号。

---

#### 2. 拓展文字 → 引用块风格 🆕

你说得对，折叠不利于阅读。改为**视觉层级分离法**——让拓展文字始终可见，但通过视觉设计明确它「从属」于主问题：

```
┌──────────────────────────────────┐
│                                  │
│  你当下的技能组合，会沦为可批量   │  ← 主问题：18sp SemiBold
│  替代的普通劳动力，还是正在成长   │     深色，最大字重
│  为市场稀缺、不可替代的独特能力   │
│  资产？                          │
│                                  │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │  ← 极细分隔线
│                                  │
│  ▎你做哪些事能轻松进入忘我心流、  │  ← 拓展：13sp Regular
│  ▎效率远超常人？有哪些事即便没有  │     左侧 3dp 绿色竖线
│  ▎物质回报，你也愿意长期投入时间  │     （blockquote 引用风格）
│  ▎精力？你在哪些领域能持续做出优  │     颜色更浅（alpha 0.65）
│  ▎于常人的成果并收获外界认可？    │     字号比主问题小一号
│                                  │
└──────────────────────────────────┘
```

**视觉层次**：
| 元素 | 字号 | 字重 | 颜色 | 识别速度 |
|------|------|------|------|---------|
| 主问题 | 18sp | SemiBold | alpha 1.0 | ⚡ 第一眼 |
| 拓展文字 | 13sp | Regular | alpha 0.65 | 👀 第二眼 |
| 绿色竖线 | 3dp 宽 | — | Primary | ➡️ 引导视线向下 |

**交互细节**：
- 拓展文字始终可见，无需用户操作
- 但视觉上明显「沉」在主问题之下
- 左侧绿色竖线 + 更小字号 + 更浅颜色 = 一看就知道是「补充」
- 每次换题时拓展文字和主问题一起做滑入动画

**对比效果**：
- 比「直接显示」（当前）：有主次区分，不喧宾夺主
- 比「折叠隐藏」（原方案）：不打断阅读流，看一眼就知道还有拓展内容

---

#### 3. 换题按钮增加视觉进度 ✅

当前：`换一问 · 1/3`（纯文字）

改进：按钮左侧加 3 个颜色圆点

```
  ┌───────────────────────────┐
  │  ● ● ○     换一问         │
  └───────────────────────────┘
    ↑ ↑ ↑
    │ │ └─ 未看（圆环空心）
    │ └─── 已看（半透明实心）
    └───── 当前（实心高亮）
```

**交互细节**：
- 3 个圆点对应每日 3 题
- 看完一轮 → 按钮文字变为「再转一轮」，圆点全部高亮
- 点击换题时按钮有微缩放动画（`animateFloatAsState`）

---

#### 4. Widget 增加桌面换题 ✅

```
当前 Widget：                    改进 Widget：
┌──────────────────┐          ┌──────────────────┐
│ 日课一问  破局人生 │          │ 日课一问  破局人生 │
│ │                 │          │ │                 │
│ 今天的问题……      │          │ 今天的问题……      │
│                   │          │  ← 换一问  ·  →  │
│   点击打开        │          │    点击打开       │
└──────────────────┘          └──────────────────┘
```

- Widget 底部新增「换一问」点击文字
- 点击通过 Glance `ActionCallback` 切换后自动刷新
- 点击「点击打开」跳转 App（保持现有功能）

---

### 🌟 P1 — 中优先级

#### 5. 分享卡片功能 ✅

在换题按钮旁新增一个分享按钮：

```
  ┌──────────┐  ┌──────────────┐
  │  换一问   │  │  📤 分享    │
  └──────────┘  └──────────────┘
```

- 将问题渲染为精美卡片图片（绿色主题背景 + 问题文字 + 日课一问水印）
- 调用系统分享（`Intent.ACTION_SEND`），用户可发微信/朋友圈/保存

---

#### 6. 年进度模块 → 圆环进度 🆕

你说得对，年进度应该留在主屏。但当前 73×5 点阵太占地方，改为**圆环进度指示器**：

```
当前（点阵矩阵 ~90dp 高）：       改进（圆环进度 ~48dp 高）：

┌──── 2026 进度 ──── 45.2% ──┐    ┌───────────────────────────────┐
│ ████████████████░░░░░░░░░░ │    │   ╭─────╮  2026 进度           │
│ 第 147 / 365 天 · 21 周    │    │  ╱  45%  ╲  45.2%  ████████░░  │
│                            │    │  ╲       ╱  第 147/365 天      │
│ ██ ██ ██ ██ ██             │    │   ╰─────╯  第 21 周            │
│ ██ ██ ██ ██ ██ … (73x5)   │    └───────────────────────────────┘
└────────────────────────────┘           ▲ 圆环进度
        ▲ 点阵占大量版面                     紧凑优雅
```

**实现方案**：

```kotlin
// 用 Canvas 绘制圆环进度
Canvas(
    modifier = Modifier.size(48.dp)
) {
    val strokeWidth = 4.dp.toPx()
    val sweepAngle = progress * 360f
    // 底色圆环
    drawArc(color = ProgressDotEmpty, ...)
    // 进度弧
    drawArc(color = ProgressDotDone, sweepAngle = sweepAngle, ...)
}
```

**右侧显示信息**：

```
圆环 │ 2026 进度 · 45.2%
     │ ████████░░░░░░░░░░░░  (细进度条)
     │ 第 147 / 365 天 · 第 21 周
```

**整体布局位置**：

```
┌──────────────────────────────────┐
│  🌱 日课一问                     │
│  5月27日 星期三                  │
│                                  │
│  ┌──── 问题卡片 ────────────┐   │
│  │  主问题                    │   │
│  │  ▎拓展文字（引用块风格）   │   │
│  └───────────────────────────┘   │
│                                  │
│  [ ● ● ○  换一问 ]  [ 📤 分享 ] │
│                                  │
│  ╭─────╮  2026 · 45.2%          │  ← 圆环进度
│  ╱  45%  ╲  第 147 / 365 天     │     紧凑一行
│  ╲       ╱  ████████░░░░░░░░░░  │
│   ╰─────╯  第 21 周             │
└──────────────────────────────────┘
```

**效果**：信息完整度不变（进度%、天数、周数），但视觉压缩到原来 1/3 的高度，且圆环比点阵**优雅一个级别**。

---

#### 7. 设置面板充实 ✅

在「更多」底部弹窗中增加：

```
┌──────────────────────────────┐
│  ⚙️ 设置                     │
│                              │
│  🌙 深色模式：跟随系统       │  ← 跟随系统/深色/浅色
│  💫 触感反馈：开启           │  ← 开关
│                              │
│  ──── 关于 ────              │
│  日课一问 v2.0              │
│  效法《论语》「吾日三省吾身」│
│  问题库：401 道灵魂拷问      │
│  来源：GitHub                │
│                              │
│  分享给朋友                  │  ← 新增推广入口
└──────────────────────────────┘
```

---

### ✨ P2 — 锦上添花

#### 8. 启动画面动画 ✅

- App 启动时，🌱 图标和问题卡片从中心淡入放大（`alpha 0→1` + `scale 0.9→1.0`）
- 时长约 400ms，营造「翻开今日日课」的仪式感

#### 9. 深色模式呼吸光效 ✅

- 深色模式背景色在 `PageBgGradientEndDark` 和稍亮色调间缓慢循环
- `InfiniteTransition` 驱动，周期约 8 秒
- 极细微，不影响文字可读性，视觉上像「森林在呼吸」

---

## 三、视觉优化清单 v2

| 当前 | 改进 | 优先级 |
|------|------|--------|
| 切换无动画 | `AnimatedContent` 左右滑入 | P0 |
| 拓展与主问题同等级 | 引用块风格（竖线 + 小字号 + 浅色） | P0 |
| 按钮 `换一问 · 1/3` | 圆点指示器 + 文字 | P0 |
| Widget 无交互 | 底部「换一问」可点击 | P0 |
| 73×5 点阵占位大 | 圆环进度指示器（~48dp） | P1 |
| 无分享能力 | 分享按钮 + 卡片图片分享 | P1 |
| 设置面板空白 | 主题切换 + 触感开关 + 完整信息 | P1 |
| 启动生硬 | 淡入放大动画 | P2 |
| 深色背景静止 | 呼吸光效 | P2 |

---

## 四、实施路线图 v2

```
Phase 1（立即做）
  ├── 卡片切换动画（AnimatedContent）
  ├── 拓展文字 → 引用块风格
  └── 按钮圆点指示器

Phase 2（本周做）
  ├── 年进度 → 圆环进度指示器
  ├── 分享卡片功能
  ├── Widget 桌面换题
  └── 设置面板充实

Phase 3（下期做）
  ├── 启动动画
  └── 深色呼吸光效
```
