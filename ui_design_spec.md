# 日课一问 — 全新 UI 设计方案

> 设计者：UI Designer  
> 设计日期：2026-05-28  
> 目标：美观 · 易用 · 有记忆点

---

## 一、设计方向

| 维度 | 选择 |
|------|------|
| Tone | **沉静沉思** — 不喧闹、不游戏化，像翻开一本精装手账 |
| 差异化点 | 卡片顶部的「今日问题」小标签 + 主问题独有的**大号细体**排版 |
| 空间哲学 | 留白 > 填充，呼吸感 > 信息密度，节奏性 > 均匀排布 |
| 色彩 DNA | 森林绿主调，暖金点缀不喧宾夺主 |

---

## 二、全新布局架构

```
┌──────────────────────────────────────────┐
│  5月28日 · 星期四                    ⋯   │  ← 信息行（日期 + 更多）
├──────────────────────────────────────────┤
│  ┌────────────────────────────────────┐  │
│  │  ● 今日问题                       │  │  ← 小标签
│  │                                    │  │
│  │  你当下的技能组合，会沦为可批量    │  │
│  │  替代的普通劳动力，还是正在成长    │  │  ← 主问题 20sp/500
│  │  为市场稀缺、不可替代的独特能力    │  │
│  │  资产？                            │  │
│  │  ─ ─ ─ ─                          │  │  ← 细分割线
│  │  • 你做哪些事能轻松进入忘我心流、  │  │
│  │    效率远超常人？                  │  │  ← 拓展 13sp/400
│  └────────────────────────────────────┘  │
│                                          │
│  ┌──── ● ● ○ 换一问 ──┐  ┌── ↑ ──┐  │  ← 控制行（按钮+分享）
│  └────────────────────────┘  └───────┘  │
│                                          │
│  ┌─ ◕ 2026进度·52.1% ──────────────┐   │
│  │  ████████░░ 第148/365天 · 21周   │   │  ← 年进度（紧凑行）
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

### 三大关键变化

| 当前 | 改后 | 理由 |
|------|------|------|
| 顶部 🌱 大字标题 | 顶部轻量日期行 | 🌱 首次打开有新鲜感，但每日使用会疲劳。日期更实用，视觉噪音更小 |
| 问题卡片顶部留 15% 空白 | 卡片顶部加「今日问题」小标签 | 标签给问题赋予「每日仪式感」，同时视觉锚定 |
| 控制按钮 + 进度两个独立卡片 | 控制按钮横贯整宽 + 进度条紧随其后 | 减少碎片感，视觉流更顺畅 |

---

## 三、设计 Token 系统

### 3.1 色彩系统（保留现有基调，优化对比度）

```kotlin
// 新增 Token 示例
val CardLabelText = Color(0xFF888780)     // 小标签文字
val CardLabelDot = Color(0xFF2E7D32)      // 小标签圆点  
val DividerLine = Color(0xFFD3D1C7)       // 分隔线
val ProgressBarEmpty = Color(0xFFC8E6C9)  // 进度条底色
val ProgressBarFill = Color(0xFF2E7D32)   // 进度条填充
```

### 3.2 排版系统（增大层级差异）

| 元素 | 字号 | 字重 | 行高 | 当前值 |
|------|------|------|------|--------|
| 日期行 | 13sp | Regular | — | 无独立字号 |
| 小标签 | **12sp** | Medium | — | 无 |
| **主问题** | **20sp** | **Medium(500)** | **32sp** | 18sp/SemiBold |
| 拓展文字 | **13sp** | Regular | **20sp** | 14sp |
| 按钮文字 | 14sp | Medium | — | 14sp/SemiBold |
| 进度标签 | 13sp | Medium | — | 12sp |
| 进度帮助文字 | 11sp | Regular | — | 11sp |

### 3.3 间距系统

| 位置 | 值 | 说明 |
|------|-----|------|
| 卡片内边距 | 水平 32dp, 垂直 28dp | 保持呼吸感 |
| 主问题→分隔线 | 20dp | 足够区分 |
| 分隔线→拓展 | 14dp | 视觉连接 |
| 控制行间距 | 16dp | 模块间缓冲 |
| 进度行间距 | 12dp | 紧凑跟随 |

---

## 四、组件拆分与职责

### QuestionCard（问题卡片）

**Props**:
- `question: Question` — 问题数据
- `primaryColor: Color` — 主题色
- `onShare: () -> Unit`
- `onSwitch: () -> Unit`

**States**:
- `normal`: 默认，问题 + 拓展可见
- `switching`: AnimatedContent 过渡中
- `empty`: 无拓展时隐藏分隔线和下半部分

### ProgressFooter（底部进度栏）

合并当前 YearProgressCard 和 BottomBar 为一个组件。

**Props**:
- `currentIndex: Int` — 当前题号 (0-based)
- `totalCount: Int` — 总题数
- `progress: Float` — 年进度 (0..1)
- `onSwitch: () -> Unit`

**内部组件**:
- `DotIndicator(currentIndex, totalCount)` — 圆点进度
- `CircularRing(progress)` — 圆环进度
- `SwitchButton(onClick)` — 换一问按钮

---

## 五、交互细节

### 5.1 换题动画（保留）

```
旧问题 ← 左滑出  |  新问题 → 右滑入
                 350ms 缓动，带淡入淡出
```

### 5.2 按钮反馈

- 点击「换一问」：按钮微缩放 + 震动
- 圆点同步变化：当前题圆点最高透明度
- 换到最后一题后，自动从第 1 题重新开始（循环）

### 5.3 启动体验

- App 打开：200ms 停顿 → 500ms 淡入
- 卡片从 scale(0.97) → scale(1.0) 微弹

---

## 六、Widget 设计（已实现）

当前 Widget 设计已经符合新方案：

```
┌────────────────────┐
│ 日课一问·破局人生   │  ← 轻量标题
│                    │
│ 今天的问题……        │  ← 15sp 正文
│                    │
│              换一问 │  ← 底部右对齐操作
└────────────────────┘
```

---

## 七、实施优先级

```
Phase 1（核心体验）
  ├── 移除顶部 🌱 标题，改为轻量日期行
  ├── 卡片顶部加「今日问题」小标签
  ├── 主问题字号 18sp → 20sp，行高 28sp → 32sp
  └── 控制按钮改为通栏宽，进度条紧随其后

Phase 2（组件重构）
  ├── 提取 ProgressFooter 合并组件
  ├── 提取 DotIndicator 为独立可复用组件
  └── 更新分享弹窗样式（对齐新设计）

Phase 3（细微打磨）
  ├── 启动动画 scale(0.97→1.0) 微弹
  ├── 色彩对比度微调
  └── 圆环进度与卡片对齐
```

---

## 八、你需要决定的

1. **顶部标题行**：去掉 🌱 只留日期，还是保留 🌱 但缩小字号？
2. **「今日问题」标签**：卡片顶部加这个标签，还是保留现在的裸卡片？
3. **主问题字号**：我建议 20sp Medium（比当前 18sp SemiBold 更大但更轻），你觉得？
4. **控制栏**：换一问按钮和分享按钮放在同一行，还是分开上下行？
